import React from "react";

class Counter extends React.Component {
  render() {
    const counter = "Counter";
    return <h1 style={{ color: "white" }}>{counter}</h1>;
  }
}

export default Counter;
